{% extends 'TopxiaWebBundle:Settings:layout.html.twig' %}

{% block title %}个人主页背景 - {{ parent() }}{% endblock %}

{% set side_nav = 'picture' %}
{% set script_controller = 'course-manage/picture' %}
{% block main %}

<div class="panel panel-default panel-col">
  <div class="panel-heading">
      个人主页背景
  </div>

  <div class="panel-body">
      {{ web_macro.flash_messages() }}

      <div class="form-group clearfix">
        <div class="col-md-offset-2 col-md-8 controls">
          <img class="img-responsive" src="{{ filepath(user.picture, 'course.png') }}" />
        </div>
      </div>

      <div class="form-group clearfix">
        <div class="col-md-offset-2 col-md-8 controls">
          <p class="help-block">你可以上传jpg, gif, png格式的文件, 图片建议尺寸至少为1400x340。<br>文件大小不能超过<strong>2M</strong>。</p>
        </div>
      </div>

      <div class="form-group clearfix">
        <div class="col-md-offset-2 col-md-8 controls">
          <a 
          id="upload-picture-btn"
          class="btn btn-fat btn-primary" 
          data-upload-token="{{ upload_token('tmp', 'image') }}"
          data-goto-url="{{path('settings_picture_crop')}}"
          >选择要上传的图片</a>
        </div>
      </div>

  </div>
</div>

{% endblock %}

